# Use this when the dev proxy doesn't reach the Node API (e.g. Codespaces, or app opened on a different port).
# Point the client directly at the backend so API responses are JSON, not HTML.
# Restart the client (yarn start) after changing.
REACT_APP_API_URL=http://localhost:5000
